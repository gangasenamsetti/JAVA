  Node newNode=new Node(-1);
        Node temp=newNode;
        while(head!=null){
            temp.next=head;
            head=head.next;
            temp=temp.next;
        }
        return newNode.next;
